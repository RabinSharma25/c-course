#include<stdio.h>

int main(){

    int a ;
    printf(" Enter a \n");
    scanf("%d",&a);
     // one linear if -- else statment 
     (a<3)?printf("a is les than 3"): printf("a is not less than 3");

    return 0;
}