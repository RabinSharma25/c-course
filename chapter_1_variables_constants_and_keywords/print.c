#include<stdio.h>
int main(){

    int a = 4; 
    float b =3.4;
    char c = 'c';
    int d = 54;

    printf("The value of int a is %d \n",a  ); // prints the value of a ; use %d for printing integers 
    printf("The value of int b is %f \n", b); // prints the value of b; use %f for printing floats 
    printf("The value of int c is %c \n", c); // prints the value of b; use %c for printing floats 
    printf("The value of a + d is %d ", a+d);

    // note : \n (backshash) prints the line in new line....
    return 0;

    }
