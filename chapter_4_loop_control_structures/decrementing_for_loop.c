#include<stdio.h>
int  main(){
   

   for (int i= 5;i;i--){ // any non zero value is true in the condition , this loop will run unless i becomes 0;;;s

       printf("the value of i is %d\n",i);
   }
    return 0;
}